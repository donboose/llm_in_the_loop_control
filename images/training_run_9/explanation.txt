Gradient SNR = 0.479: The learning signal-to-noise ratio is below 1.0. The noise (reward_std/sqrt(64)) dominates the signal (|mean_reward|). This means the gradient direction is essentially random - the optimizer can't tell which direction to go. This is the FUNDAMENTAL problem.

No goal bonus ever: The reward never exceeds +1.2, but the goal bonus would be +5 - so the agent isn't actually reaching the goal.
